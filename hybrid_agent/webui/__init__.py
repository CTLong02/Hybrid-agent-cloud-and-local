"""Streamlit web UI for the hybrid coding agent.

Launch with:

    hybrid-agent webui -c config.yaml -t tasks.md

which boils down to `streamlit run hybrid_agent/webui/app.py` with the config
and tasks file paths forwarded via environment variables.
"""
